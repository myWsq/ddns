type Domain {
	id: Int!
	name: String!
	rr: String!
}

input DomainDto {
	name: String!
	rr: String!
}

type Mutation {
	addDomain(providerID: Int!, domain: DomainDto!): Domain! @auth
	deleteDomain(id: Int): Domain! @auth
	updateDomain(id: Int, domain: DomainDto!): Domain! @auth
}
